# macOS
.DS_Store

# Editor / agent state
.claude/
.vscode/
.idea/
*.swp

# Node (for when Electron/React land)
node_modules/
dist/
build/
*.log
npm-debug.log*

# Local env
.env
.env.local
.env.*.local

# Server-side session snapshots (one JSON per session, see
# scripts/server/sessions.mjs). Per-developer scratch state, not source.
sessions/
